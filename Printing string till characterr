#include <stdio.h>
#include <string.h>
int main() {
    char S[101]; 
    char C;
    scanf("%s", S);
    scanf(" %c", &C); 
    for (int i = 0; i < strlen(S); i++) {
        if (S[i] == C) {
            break;
        }
        printf("%c", S[i]);
    }
    return 0;
}
