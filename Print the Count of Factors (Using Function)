#include<stdio.h>
int loga(int a)
{
    int b=1,d=0;
    while(b<=a)
    {
      if(a%b==0)
      {
          d++;
      }
      b++;
    }
    printf("%d",d);
}
int main()
{
    int a;
    scanf("%d",&a);
    loga(a);
}
