#include <stdio.h>
#include <string.h>
int main() {
    char S[1000];
    scanf("%s", S);
    int length = strlen(S);
    if (length > 2) {
        for (int i = 1; i < length - 1; i++) {
            printf("%c", S[i]);
        }
    }
    return 0;
}
