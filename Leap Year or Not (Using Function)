#include<stdio.h>
int loga(int a)
{
    if((a%400==0)||(a%100!=0&&a%4==0))
    return 0;
    
    return 1;
}
int main()
{
    int a;
    scanf("%d",&a);
    printf("%s",loga(a) ? "Not a Leap Year" : "Leap Year");
}
